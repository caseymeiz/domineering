import { defaultSize } from '../board/default';
import { createGrid } from '../board/default';


export const defaultVertical = createGrid(defaultSize);
export const defaultVerticalHovered = createGrid(defaultSize);

export const defaultHorizontal = createGrid(defaultSize);
export const defaultHorizontalHovered = createGrid(defaultSize);


// defaultVertical[1][3] = true;
// defaultHorizontal[3][1] = true;



